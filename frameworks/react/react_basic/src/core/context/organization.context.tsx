// TODO - styles context for Rick and Morty in advanced
/* import React, { useState, useEffect, createContext } from 'react';

interface GithubContextModel {
    organizationName: string,
    // setOrganizationName: (orgName: string) => string
}

const organizationContext:GithubContextModel = {
  organizationName: 'lemoncode',
};

const GithubContext = React.createContext<GithubContextModel>(organizationContext);

export default GithubContext; */
