import { Theme, createTheme } from '@material-ui/core/styles';

export const theme: Theme = createTheme({
  palette: {
    primary: {
      main: '#3f51b5',
    },
    secondary: {
      main: '#f50057',
    },
  },
});
